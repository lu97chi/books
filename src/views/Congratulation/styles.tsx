import { StyleSheet } from 'react-native'
import { GreenPrimary } from '../../../assets/constants'

export default StyleSheet.create({
    Container: {
        backgroundColor: GreenPrimary,
        flex: 1,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        paddingBottom: 50
    }
})
