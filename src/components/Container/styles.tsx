import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ContainerStyles: {
        paddingHorizontal: 8,
        paddingVertical: 12,
        display: 'flex',
        flex: 1
    }
})